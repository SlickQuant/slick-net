@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find required dependencies
find_dependency(Boost CONFIG COMPONENTS beast context)
find_dependency(OpenSSL CONFIG)
find_dependency(slick-stream-buffer-multiplexer 2.0.0 CONFIG)
find_dependency(slick-dynamic-buffer 1.0.2 CONFIG)

# Include the targets file
include("${CMAKE_CURRENT_LIST_DIR}/slick-net-targets.cmake")

# Exported target:
# - slick::net (static library)

check_required_components(slick-net)
